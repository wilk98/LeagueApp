@model IEnumerable<LeagueApp.Models.League>

<h2>Ligi</h2>
<ul>
    @foreach (var league in Model)
    {
        <li><a href="@Url.Action("Details", "Leagues", new { id = league.LeagueId })">@league.Name</a></li>
    }
</ul>
